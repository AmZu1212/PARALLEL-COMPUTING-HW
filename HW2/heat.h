void init_heap();
void delete_heap();
extern double* t_old;
extern double* t_new;
extern size_t n_x;
extern size_t n_y;
void heat (int mode);
//compile using:
// gcc -std=c++11 -o3 -o <binary filename>

//Make sure you use the exact file names, function names and class names mentioned below. 
//Otherwise, you will face compilation errors during automatic testing.

// only use TBB
// 100% auto graded.
//


// to submit: tree_barrier_impl.h (ex1), heat.cpp (ex2)
// local testing is better



